programa
    int i = 0;
    int limite = 10;
inicio
    escreva("Digite um número: ");
    leia(limite);

    para (i = 0; i < limite; i = i + 1) {
        se (i % 2 == 0) entao {
            continue;
        }

        escreva(i);
    }
fimprog.